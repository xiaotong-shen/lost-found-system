package interface_adapter.admin;

import interface_adapter.ViewModel;

public class AdminViewModel extends ViewModel<AdminState> {
    public AdminViewModel(String viewName) {
        super(viewName);
    }
}